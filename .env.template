# 数据库配置
# SQLite (默认，用于开发):
database_url="sqlite+aiosqlite:///./feishu-bot.sqlite"
# 或使用内存数据库进行测试:
# database_url="sqlite+aiosqlite:///:memory:"
# 或使用 PostgreSQL (生产环境):
# database_url="postgresql+asyncpg://user:password@localhost/feishu_bot"

# 数据库连接池设置 (可选)
# database_echo=False
# database_pool_size=5
# database_pool_timeout=30
